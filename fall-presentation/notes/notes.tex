\documentclass{extarticle}

\usepackage{mathtools}
\usepackage{libertinus}
\usepackage{fancyhdr}
\usepackage{microtype}
\usepackage{geometry}
\usepackage{parskip}
\geometry{
  left=.75in, right=2in, top=.5in, bottom=.5in,
  includeheadfoot, headheight=1em, headsep=1em,
}
\renewcommand\familydefault\sfdefault
\pagestyle{fancy}
\fancyhf{}
\renewcommand\headrulewidth{0pt}

\usepackage{tikz}
\usetikzlibrary{calc}
\usetikzlibrary{positioning}
\usetikzlibrary{arrows.meta}
\usetikzlibrary{circuits}
\usetikzlibrary{circuits.logic}
\usetikzlibrary{circuits.logic.US}
\usepackage{tcolorbox}

\tikzset{
  every picture/.style={
    circuit logic US
  },
  input/.style={
  },
  output/.style={
  },
  input/.pic={
  },
  joint/.style={
    circle,
    fill=black,
    inner sep=0pt,
    minimum size=1.5pt,
  },
  wire/.style={
    in=west, out=east,
    rounded corners=1em/4,
    to path={
      (\tikztostart) coordinate[joint]
      -- ($ (\tikztostart)!1/4!(\tikztostart -| \tikztotarget) $) 
      -- ($ (\tikztotarget)!1/4!(\tikztostart |- \tikztotarget) $) 
      -- (\tikztotarget) coordinate[joint]
    },
    preaction={
      draw=white,
      line width=1.5pt,
    },
  },
}

\begin{document}

\section{Intro: you can solve it, but can you play it?}

no idea what to say here

\section{Circuits and the ``lights on'' puzzle}


\section{The ``lights on'' game}

\section{The graph coloring puzzle}

\section{The graph coloring game}

\section{Conclusion}

\end{document}
